# LED 遅延（ジッタ）計測 結果分析

計測スケッチ: `LED_delay_measure.pde`（Processing、BPM 同期 LED の点灯タイミングを CSV 記録）
データ: `jitter_log_*.csv` 24 ファイル（2026-07-07 〜 2026-07-08、有効 17 ファイル・計 **1,544 拍**）

jitter_ms = 実際の点灯時刻 − 期待点灯時刻（正 = 遅れ、負 = 早い）

## 1. 全体サマリー

| 指標 | 値 |
|---|---|
| サンプル数 | 1,544 拍 |
| 平均 | **+7.6 ms**（常に遅れ方向） |
| 標準偏差 | 3.1 ms |
| 最小 / 最大 | 0 ms / +20 ms |
| p95（絶対値） | 12 ms |

**負のジッタ（早く点灯）は 1 件もない**。ズレは 0〜20 ms の範囲に収まり、
大半（約 70%）が 6〜10 ms に集中している。

## 2. BPM 別の統計

| BPM | 拍間隔 | n | 平均 | 標準偏差 | max | p95 |
|---:|---:|---:|---:|---:|---:|---:|
| 40 | 1500 ms | 14 | +2.5 ms | 2.2 ms | 9 ms | 9 ms |
| 53 | 1132 ms | 20 | +3.4 ms | 3.5 ms | 16 ms | 9 ms |
| 60 | 1000 ms | 62 | +4.0 ms | 4.5 ms | 20 ms | 16 ms |
| **66** | **909 ms** | **1201** | **+8.6 ms** | **2.1 ms** | **17 ms** | **12 ms** |
| 79 | 759 ms | 95 | +8.2 ms | 1.7 ms | 15 ms | 11 ms |
| 80 | 750 ms | 2 | +1.5 ms | 0.5 ms | 2 ms | 2 ms |
| 100 | 600 ms | 10 | +1.6 ms | 2.2 ms | 7 ms | 7 ms |
| 103 | 582 ms | 140 | +2.0 ms | 1.3 ms | 8 ms | 4 ms |

平均ジッタは BPM によって約 +2 ms 〜 +9 ms と系統的に変わる（後述の原因参照）。

## 3. 分布（全 1,544 拍、1 ms 刻み）

```
  0ms | ######. 31
  1ms | ###########. 58
  2ms | ################. 82
  3ms | ########. 44
  4ms | ####. 21
  5ms | ####. 22
  6ms | ############ 60
  7ms | ###################################################### 270
  8ms | ##########################################################################. 371
  9ms | #####################################################. 269
 10ms | ############################. 141
 11ms | ############. 61
 12ms | #########. 48
 13ms | #### 20
 14ms | ### 15
 15ms | ###. 16
 16ms | ##. 11
 17ms | . 2
 18ms | . 1
 20ms | . 1
```

二峰性の分布。7〜9 ms の大きな山はサンプルの大半を占める BPM 66、
0〜3 ms の小さな山は BPM 103 などその他の BPM に対応する。

## 4. 原因の考察: 60fps フレーム量子化

LED の点灯判定は Processing の `draw()` 内で行っており、`draw()` は 60fps
（フレーム周期 16.7 ms）でしか回らない。そのため期待時刻を過ぎても、
次のフレームが来るまで点灯できず、**ズレは常に遅れ方向・最大約 1 フレーム分**になる。

さらに、点灯時刻はフレーム境界に量子化されるため、次の期待時刻
（= 実点灯時刻 + 拍間隔）とフレーム境界の位相差は毎拍ほぼ一定になり、
**平均ジッタは「(−拍間隔) mod 16.7 ms」で予測できる**。実測との比較:

| BPM | 拍間隔 | 理論ジッタ | 実測平均 | 差 |
|---:|---:|---:|---:|---:|
| 40 | 1500 ms | 0.0 ms | +2.5 ms | +2.5 |
| 53 | 1132 ms | 1.3 ms | +3.4 ms | +2.1 |
| 60 | 1000 ms | 0.0 ms | +4.0 ms | +4.0 |
| 66 | 909 ms | 7.7 ms | +8.6 ms | +0.9 |
| 79 | 759 ms | 7.7 ms | +8.2 ms | +0.5 |
| 80 | 750 ms | 0.0 ms | +1.5 ms | +1.5 |
| 100 | 600 ms | 0.0 ms | +1.6 ms | +1.6 |
| 103 | 582 ms | 1.3 ms | +2.0 ms | +0.7 |

全 BPM で理論値 +1〜4 ms に収まっており、量子化仮説と整合する
（残差の +1〜4 ms はフレーム周期の揺らぎと描画処理の遅延分）。
つまり **観測されたジッタの支配要因は BPM 検出やシリアル通信ではなく、
描画ループの時間分解能**である。

## 5. 結論

- ズレは最大でも 20 ms、95% は 12 ms 以内。**音楽と光の同期ズレとして人間が
  知覚できる閾値（一般に 20〜30 ms）を下回っており、実用上問題ないレベル**。
- ジッタの原因は 60fps の描画ループによる量子化で、システム固有の遅延や
  BPM 検出精度の問題ではない。

### 改善するなら（必要な場合のみ）

1. `setup()` で `frameRate(240);` を指定する — 時間分解能が 16.7 ms → 約 4 ms に
   なり、平均ジッタは 2 ms 程度まで下がる見込み。変更 1 行で最も手軽。
2. LED 判定を `draw()` から独立したスレッド（`Thread` + `millis()` 監視）に
   移す — フレームレート非依存で 1 ms 精度が狙えるが、実装コストが上がる。

---
*集計方法: `jitter_log_*.csv` の `jitter_ms` 列を Python（標準ライブラリ）で
ファイル横断集計。標準偏差は母標準偏差、p95 は絶対値ソートの 95 パーセンタイル
（計測スケッチ内 `printJitterSummary()` と同一の定義）。*
